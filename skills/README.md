Skills folder

Place individual Copilot skill packages here. Each skill should live in its own subdirectory with a manifest (e.g., skill.yaml or skill.json), handlers, and tests.

Example layout:
skills/my-skill/
  - skill.yaml
  - handler.js
  - README.md